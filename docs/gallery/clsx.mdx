---
sidebar_position: 3
description: Transforms all `x-clsx` props to `className` with [clsx](https://www.npmjs.com/package/clsx) applied.
---

# clsx

Transforms all `x-clsx` props to `className` with [clsx](https://www.npmjs.com/package/clsx) applied.

import Sandpack from '../../src/components/Sandpack.js'
import { removeIndent } from '../../src/components/util.js'

<Sandpack
  css={`
    body {
      @apply flex h-screen justify-center items-center;
    }
  `}
  features={['clsx()']}
  files={{
    '/App.jsx': removeIndent(`
  export default function App() {
    return (
      <button x-clsx={["bg-blue-500", "text-white"]}>
        Hello
      </button>
    )
  }
`)
  }}
/>

## Installation

```bash
npm install react-beyond @react-beyond/clsx
```

## Usage

```tsx
import { Beyond } from 'react-beyond'
import { classFor } from '@react-beyond/clsx'

<Beyond features={[clsx()]}>
  <App />
</Beyond>
```

## Options

- `id` - The id of the feature HOC. Defaults to `"clsx"`.
